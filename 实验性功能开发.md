# 实验性功能开发

本文记录当前作者页显示相关实验性功能的进度、实现方式和后续观察点。

## 当前状态

目前作者页相关控制已经从 Pixiv 页面内悬浮窗迁移到浏览器扩展 popup 中。

popup 中的页签为：

- 标记
- 作者页
- 排除
- 备份

其中 `作者页` 页签包含：

- `每行`：控制作者页作品列表每行显示数量。
- `最低页数`：隐藏页数少于指定值的作品。
- `高清缩略图`：尝试替换作者页卡片缩略图，目前效果仍不稳定，暂时不继续深入。
- `实验性悬停预览`：用于验证是否能通过 Pixiv 接口获取到更高清的作品首图。

## 已完成调整

### 1. 移除作者页页面内悬浮控制窗

之前作者页右侧会显示一个 `作者页显示` 悬浮窗，用于控制每行数量、最低页数和高清缩略图。

现在已改为：

- 作者页不再显示这个控制悬浮窗。
- 控制入口移动到扩展 popup 的 `作者页` 页签。
- 作品页右侧的 `作者作品速览` 悬浮面板保留，不属于本次移除范围。

相关实现位置：

- `src/content/author-panel.js`
- `src/content/author-panel.css`
- `src/popup/popup.html`
- `src/popup/popup.js`
- `src/popup/popup.css`

### 2. 作者页设置迁移到 shared settings

作者页显示设置现在保存在 shared `settings` 中，而不是 `pvmAuthorPanelUi`。

新增/使用的设置字段：

```js
authorPageGridColumns
authorPageMinPageCount
authorPageUseHighResThumbnails
authorPageHoverPreviewEnabled
```

默认值定义在：

```text
src/shared/constants.js
```

popup 保存设置时通过：

```js
PVM.storage.saveSettings(...)
```

content script 通过 `chrome.storage.onChanged` 监听 `settings` 变化，并重新应用作者页增强逻辑。

### 3. 兼容旧的悬浮窗设置

旧版本曾把以下字段存到 `pvmAuthorPanelUi` 中：

```js
homeGridColumns
homeMinPageCount
homeUseHighResThumbnails
```

当前实现中保留了兼容迁移逻辑：

- 如果 shared `settings` 中还没有新字段；
- 且旧 `pvmAuthorPanelUi` 中有对应值；
- 则尝试迁移到 shared `settings`。

没有迁移旧悬浮窗的位置和宽度，因为作者页悬浮窗已经移除。

## 作者页增强实现方式

作者页增强仍由 `src/content/author-panel.js` 负责。

核心流程：

1. 识别作者页路由：

```text
/users/{id}
/users/{id}/artworks
/users/{id}/illustrations
/users/{id}/manga
```

2. 扫描页面内作品链接：

```js
document.querySelectorAll('a[href*="/artworks/"]')
```

3. 通过稳定 URL 解析获取作品 ID。

4. 沿 DOM 向上查找作品卡片和共同父容器。

5. 选择包含作品卡片数量最多的父容器作为作品网格容器。

6. 给该容器添加：

```css
.pvm-author-home-grid
```

7. 通过 CSS 变量控制列数和缩放：

```css
--pvm-author-home-columns
--pvm-author-home-scale
```

8. 如果设置了最低页数，则用 Pixiv 作者作品详情接口补充 `pageCount`，并给不满足条件的卡片添加：

```css
.pvm-hidden-page-count-artwork
```

## 高清缩略图当前状态

`高清缩略图` 目前仍保留在 popup 的 `作者页` 页签中，但实际效果不稳定。

当前实现尝试做过：

- 从 Pixiv 作者作品详情中提取更高清的候选图。
- 替换卡片中的：
  - `src`
  - `srcset`
  - `sizes`
  - `data-src`
  - `data-srcset`
- 如果存在 `<picture><source>`，也尝试处理 source 的相关属性。
- 关闭或离开作者页时恢复原始属性。

但实际页面上仍可能看不出明显变化，原因可能包括：

- Pixiv 作者列表接口返回的图片本身并不比当前页面展示的候选图更清晰。
- Pixiv 可能有自己的 lazy loading / React re-render 逻辑覆盖属性。
- 浏览器可能继续从其它 source/candidate 中选择原来的资源。
- 当前页面卡片图可能已经使用了接口能拿到的最高缩略图版本。

因此暂时不继续深入这个批量替换功能，转而通过悬停预览验证能否拿到更高质量图。

## 实验性悬停预览

### 目标

`实验性悬停预览` 用于验证：

> 当鼠标悬停在作者页某个作品卡片上时，是否能通过 Pixiv 单作品接口拿到更高清的首图，并以独立浮层展示。

这个功能不替换作者页卡片缩略图，只做临时预览。

### 开关位置

扩展 popup：

```text
作者页 -> 实验性悬停预览
```

默认关闭。

### 实现方式

实现位置：

```text
src/content/author-panel.js
src/content/author-panel.css
```

核心思路：

1. 作者页增强流程运行时，获取当前页面作品 entries。

2. 如果：

```js
settings.authorPageHoverPreviewEnabled === true
```

则给作品卡片绑定 hover 事件。

3. 使用 dataset 避免重复绑定：

```js
data-pvm-hover-preview-bound="true"
```

4. 鼠标进入卡片后，延迟约 140ms 再开始预览请求，避免快速划过时频繁请求。

5. 请求接口：

```text
/ajax/illust/{artworkId}
```

6. 优先从接口结果中选择图片：

```js
body.urls?.original
body.urls?.regular
body.urls?.small
body.url
body.urls?.thumb_mini
body.thumbnail
body.thumbnailUrl
```

7. 如果接口请求失败或没有候选图，则 fallback 到已缓存的作者作品详情：

```js
state.workMap[id]?.highResImage
state.workMap[id]?.image
```

8. 使用内存缓存避免同一作品重复请求：

```js
hoverPreviewCache
```

9. 用请求 token 避免快速移动鼠标时旧请求晚返回覆盖新预览：

```js
hoverPreviewToken
```

10. 预览使用独立 DOM：

```html
<aside id="pvm-author-hover-preview">
  ...
</aside>
```

该浮层追加到 `document.documentElement`，不改写 Pixiv 原作品卡片 DOM。

### 浮层行为

悬停预览浮层：

- `position: fixed`
- `pointer-events: none`
- 会跟随鼠标附近显示
- 会尽量 clamp 到 viewport 内，避免溢出屏幕
- 显示 loading 文案
- 图片加载后显示图片和标题
- 失败时显示错误文案

隐藏时机：

- 鼠标离开卡片
- 页面滚动
- 窗口 resize
- 离开作者页
- 关闭 `实验性悬停预览`
- 作者页增强清理时

### 预期用途

这个功能主要用于人工观察：

- `/ajax/illust/{id}` 是否能拿到 `original` 或更高质量图片。
- 这些图片是否真的比作者页卡片缩略图更清晰。
- 请求速度是否能接受。
- Pixiv 是否允许这些图片直接在页面中展示。

如果悬停预览能稳定显示足够清晰的图，后续可以再考虑把同样的图片来源用于 `高清缩略图` 批量替换。
